# Worker-specific development env vars. The backend's .env.development is loaded first (see the
# dev script), so database connection strings etc. are inherited from there.

HEXCLAVE_TEMPORAL_ADDRESS=localhost:${NEXT_PUBLIC_HEXCLAVE_PORT_PREFIX:-81}46
